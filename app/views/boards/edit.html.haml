%h1
  Editing 
  = @board.name

- form_for(@board) do |form|
  = form.error_messages

  %p
    = form.label :name
    = form.text_field :name

  %p
    = form.submit 'Update'

= link_to 'Show', @board
|
= link_to 'Back', boards_path
